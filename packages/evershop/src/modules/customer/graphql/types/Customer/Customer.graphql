"""
Represents a customer
"""
type Customer {
  customerId: Int!
  uuid: String!
  status: Int!
  email: String
  fullName: String!
  createdAt: Date!
  languageCode: String!
  currencyCode: String!
}

extend type Query {
  currentCustomer: Customer
}
