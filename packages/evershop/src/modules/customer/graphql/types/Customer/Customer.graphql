"""
Represents a customer
"""
type Customer {
  customerId: Int!
  uuid: String!
  email: String
  firstName: String
  lastName: String
  createdAt: Date!
  avatarUrl: String
}

extend type Query {
  currentCustomer: Customer
}
