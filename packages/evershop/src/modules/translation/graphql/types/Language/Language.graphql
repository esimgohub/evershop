type Language {
  id: ID!
  code: String!
  name: Int
  icon: String
}

extend type Customer {
  language: Language!
}

extend type Query {
  language(code: String): Language
  languages(filters: [FilterInput]): [Language]
}
